'use strict';

// Given a sorted array (array) and a number (V), can you print the index location of V in the array?

const array = [1, 2, 3];
const V =3;

function introTutorial(V, arr) {
    return arr.indexOf(V);
}